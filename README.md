# skill-registry

## Local Development

### Prerequisites
- Node.js

### Installation

**Backend:**
```bash
cd backend
npm install
npm run dev
```
Backend runs on http://localhost:3001. API available at `/api/skills`.

**Frontend** (in a new terminal):
```bash
cd frontend
npm install
npm run dev
```
Frontend runs on http://localhost:5173.
